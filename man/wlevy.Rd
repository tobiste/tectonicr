% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{wlevy}
\alias{wlevy}
\alias{dwlevy}
\title{Wrapped Lévy Distribution}
\usage{
dwlevy(theta, mean, c, axial = FALSE, tol = 1e-08, P0 = 100, P.max = 5000)
}
\arguments{
\item{theta}{numeric. Angular value in degrees}

\item{mean}{numeric. The mean vector in degrees.}

\item{c}{numeric. Scale factor of the Lévy distribution. Small values indicate concentrated data near the mean.}

\item{axial}{logical. Whether the data are axial, i.e. \eqn{\pi}-periodical
(\code{TRUE}, the default) or directional, i.e. \eqn{2 \pi}-periodical (\code{FALSE}).}

\item{tol}{numeric. The precision in evaluating the distribution function or the quantile.}

\item{P0, P.max}{numeric. Arguments Control how the truncated series
(the Fourier/characteristic-function sum) is grown and when it gives up.
In practice you'll almost never touch either one, as for typical \code{c} (\eqn{c \le 0.1})
convergence happens within the first one or two doublings, well under \code{P0}.}
}
\description{
Probability density function of the wrapped Lévy distribution
}
\examples{
set.seed(1)
x <- rwcauchy(5, mean = 90, rho = exp(-1))

dwlevy(x, mean = 80, c = 10)
}
\seealso{
\link{cunif}, \link{wnorm}, \link{wcauchy}, and \link{vonmises}
}
